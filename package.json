{
  "name": "katoassign",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prettier-check": "prettier --check \"**/*.{ts,tsx,md}\"",
    "test": "turbo run test"
  },
  "resolutions": {},
  "packageManager": "pnpm@10.10.0",
  "devDependencies": {
    "turbo": "^2.5.2"
  },
  "dependencies": {
    "next": "15.3.1"
  },
  "workspaces": [
      "packages/*",
      "apps/*"
  ]
}
