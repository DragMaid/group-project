{
  "name": "@db",
  "scripts": {
    "migrate": "prisma migrate dev --schema=schema",
    "generate": "prisma generate --schema=schema",
    "reset": "prisma migrate reset --schema=schema",
    "push": "prisma db push --schema=schema",
    "pull": "prisma db pull --schema=schema",
    "studio": "prisma studio --schema=schema",
    "format": "prisma format --schema=schema",
    "test": "pnpm jest --coverage",
    "seed": "pnpm ts-node ./seed.ts"
  },
  "dependencies": {
    "tsconfig": "workspace:*",
    "eslintconfig": "workspace:*",
    "ts-node": "^10.0.0",
    "@prisma/client": "^6.8.2",
    "prisma": "^6.8.2"
  },
  "devDependencies": {
    "ts-node": "^10.0.0",
    "@prisma/client": "^6.8.2",
    "prisma": "^6.8.2"
  }
}
